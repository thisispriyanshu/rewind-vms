## What & why

<!-- What does this change, and what problem does it solve? -->

## Checklist

- [ ] `pytest` passes on SQLite (no infrastructure needed)
- [ ] `ruff check .` and `ruff format .` are clean
- [ ] New behavior has tests
- [ ] No destructive path added to the versioned model (history stays append-only)
- [ ] No credentials or `.env` contents in the diff
